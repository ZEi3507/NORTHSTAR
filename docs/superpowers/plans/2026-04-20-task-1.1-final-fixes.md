# Task 1.1 Final Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sync breakpoints between Nav and MobileMenu, and unify link labels.

**Architecture:** Update Tailwind classes for breakpoint consistency and update text content for label unification.

**Tech Stack:** React, Tailwind CSS

---

### Task 1: Breakpoint Sync in MobileMenu

**Files:**
- Modify: `src/components/MobileMenu.tsx`

- [ ] **Step 1: Update backdrop class**
Change `md:hidden` to `lg:hidden` on the backdrop `motion.div`.

- [ ] **Step 2: Update menu panel class**
Change `md:hidden` to `lg:hidden` on the menu panel `motion.div`.

- [ ] **Step 3: Verify changes**
Ensure the mobile menu is visible on medium screens but hidden on large screens (1024px+).

### Task 2: Link Label Unification in Nav

**Files:**
- Modify: `src/components/Nav.tsx`

- [ ] **Step 1: Update "Scholars" label**
Change "Scholars" to "Find Scholars" in the desktop navigation link.

- [ ] **Step 2: Verify changes**
Ensure the desktop link now reads "FIND SCHOLARS" (case may be handled by CSS).
